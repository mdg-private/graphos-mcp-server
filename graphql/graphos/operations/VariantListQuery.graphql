query VariantListQuery($graph_id: ID!) {
  frontendUrlRoot
  graph(id: $graph_id) {
    variants {
      name
      id
      contractFilterConfigDescription
      federationVersion
      isProposal
      url
      subscriptionUrl
      routerConfig
    }
  }
}
